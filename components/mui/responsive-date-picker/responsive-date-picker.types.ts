export enum DatePickerError {
  required = 'required',
  isPast = 'isPast',
  isFuture = 'isFuture',
  isValid = 'isValid',
  invalidBirthday = 'invalidBirthday',
  checkIfIsAdult = 'checkIfIsAdult',
  isNotValidMinStartDate = 'isNotValidMinStartDate',
}
