export enum RoadAssistanceEUErrorType {
  required = 'required',
  invalid = 'invalid',
  confirmFailed = 'confirmFailed',
  isPast = 'isPast',
  createFailed = 'createFailed',
  priceRequired = 'priceRequired',
  invalidData = 'invalidData',
  isNotValidMinStartDate = 'isNotValidMinStartDate',
}
