export enum RcaErrors {
  required = 'required',
  invalid = 'invalid',
  duplicated = 'duplicated',
  isPast = 'isPast',
  createFailed = 'createFailed',
  confirmFailed = 'confirmFailed',
  priceRequired = 'priceRequired',
  invalidData = 'invalidData',
}
