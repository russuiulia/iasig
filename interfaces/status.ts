export enum Status {
  DRAFT = 'draft',
  PAID = 'paid',
  PROCESSING = 'processing',
  ISSUED = 'issued',
  COMPLETED = 'completed',
  EXPIRED = 'expired',
  FAILED = 'failed',
  REFUNDED = 'refunded',
}
